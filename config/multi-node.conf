# Master control-plane host info
MASTER_HOST_NAME=nuc-master
MASTER_HOST_IP=192.168.40.87

# Default SSH user for worker nodes
WORKER_SSH_USER=ubuntu

# Define workers as semicolon-separated entries of name,IP,ssh_key[,ssh_port]
WORKERS="worker1,192.168.40.88,$HOME/.ssh/id_rsa;worker2,192.168.40.89,$HOME/.ssh/id_rsa"
