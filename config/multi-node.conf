# Master control-plane host info
MASTER_HOST_NAME=vm1
MASTER_HOST_IP=192.168.123.164

# Define workers as semicolon-separated entries:
#   name,IP,ssh_key[,ssh_user][,ssh_port]
# If ssh_user is omitted the deploy script falls back to the current user.
WORKERS="vm2,192.168.123.184,$HOME/.ssh/id_rsa"
#WORKERS="vm2,192.168.123.184,$HOME/.ssh/id_rsa;vm3,192.168.40.XX,$HOME/.ssh/id_rsa"
